---
name: activity-pause
description: 【必須】進行中のアクティビティを完了にせず中断して寝かせる。「/ap」「/activity-pause」「一旦ここまで」「この作業いったん置いておく」「中断して別の作業に移る」「今日はここで止める」など、完了ではない中断・切り替えの意図で発動する。作業が完了したとき（activity-finish）、セッション全体の記録（sync-memory）には発動しない。このスキルを経由せずに中断目的でupdate_activityのstatus変更を直接呼んではいけない。
---

# activity-pause

進行中のアクティビティを完了にせず中断し、再開しやすい状態にして寝かせる。

## 手順

1. **対象特定**
   - このセッションでcheck-in・作成したアクティビティを対象とする
   - 対象候補が複数あれば、**1ターンだけ**どれを中断するか確認する
   - このセッションに該当がなければ、`get_activities`でactiveなアクティビティのタイトル一覧を提示し、選んでもらう（内部IDはユーザーに見せない）。対象が0件ならその旨を伝えて終了する

2. **再開メモの追記**
   - `update_activity`の`description`引数は全文置換（既存内容とのマージはない）のため、既存`description`の全文に中断メモを連結した全文を渡す
   - `get_activities`の一覧プレビューは`description`の先頭200文字のみを返す。中断メモを末尾に追記すると、既存descriptionが200文字を超える場合に一覧上で常に隠れてしまうため、中断メモは既存内容より**前**に挿入する
   - 手順1のフォールバック（`get_activities`）で対象を選んだ場合、その返り値の`description`は200字に切り詰められている。そのまま追記のベースにせず、`get_by_ids`で説明文の全文を取得してから連結する
   - 挿入形式（既存description全文の直前に置く）:
     ```
     ## 中断メモ (YYYY-MM-DD)
     - どこまで: ...
     - 次にやること: ...
     - ブロッカー: ...（あれば）

     ---
     (既存descriptionの全文)
     ```
   - 内容は会話から自明な範囲で埋める。不明な項目があれば**1ターンだけ**聞いてよい
   - 中断→再開→再中断を繰り返すと中断メモが積み重なりdescriptionが肥大化し続ける（`update_activity`はdescription変更のたびにembeddingを再生成するため、埋め込み対象テキストも単調増加する）。過去の中断メモが複数溜まってきたら、直近以外を要約・削減することを検討する

3. **statusの変更**
   - 近く（同じ日〜数日以内）に再開する見込み: `status="pending"`に戻す
   - 明確な再開時期が読めずしばらく寝かせる: `status="snoozed"`にする。snoozedは既定3日（環境変数で変更可）経過後、次に`get_activities`が呼ばれたタイミングで`pending`へ復活する（バックグラウンドタイマーによる自動遷移ではなく、呼び出し時にまとめて反映されるlazy evaluationのため、`get_activities`が呼ばれるまでは3日経過後も`snoozed`のままになりうる）
   - 期限を決めず長期棚上げする: `status="shelved"`にする。shelvedは自動復活せず、明示的にstatusを変更するまで`active`一覧に出てこない
   - 判断に迷う場合はユーザーに選んでもらう
   - 手順2の`description`更新とこのstatus変更は、`update_activity`の同一呼び出しにまとめて渡す（snoozed状態のアクティビティはstatusを指定せず他フィールドのみ更新すると自動的に`pending`へ復活する仕様があり、呼び出しを分けると意図しないstatus遷移が起きうる）

4. **一言報告**
   - 中断したことと、再開は`check-in`から行える旨を伝える

## 注意

- 軽量原則: 記録の棚卸しはしない（それは`sync-memory`の領分）。ログは残さない
- `activity-finish`との境界: 作業が完了したら`activity-finish`、戻ってくる前提の中断が`activity-pause`
